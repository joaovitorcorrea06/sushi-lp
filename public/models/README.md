Place the exported hero model here as `hero.glb` and update
`src/config/landing-content.js` by setting `heroScene.modelUrl` to
`/models/hero.glb`.

For the final premium hero, prefer a local `.glb` here over an embedded
Sketchfab iframe. The local asset gives full control of lighting, framing,
scroll choreography, and loading behavior.
